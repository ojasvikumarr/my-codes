package DynamicProgramming;
import java.util.* ;
public class LongestDIvisibleSubset {
    public void recurr( int idx , int arr[] , ArrayList<Integer> set , ArrayList<ArrayList<Integer>> ans){
        if(idx == arr.length){
            ans.put(set);
            new
        }
    }
}
